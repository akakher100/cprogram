//Write a program that takes two integers as input and prints their sum. 
#include <stdio.h>
int main()
{
      int a, b, c;
      printf("Enter two number: ");
      scanf("%d%d",&a,&b);
      c = a + b;
      printf("Sum = %d", c);
      return 0;
}

